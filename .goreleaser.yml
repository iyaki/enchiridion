version: 2
project_name: enchiridion

before:
  hooks:
    - go mod tidy

builds:
  - id: enchiridion
    main: ./cmd/enchiridion
    binary: enchiridion
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    # Spec (integration.md — Distribution): linux amd64/arm64, macos arm64.
    # Also avoids syscall.Flock, which has no Windows implementation.
    ignore:
      - goos: darwin
        goarch: amd64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: enchiridion
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - "^refactor:"
      - "Merge branch"

release:
  draft: false
  prerelease: auto
